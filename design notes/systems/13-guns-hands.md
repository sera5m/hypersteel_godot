# Gun hands

Was missing from 13. Now law.

LMB primary. RMB secondary/alt on the same gun. MMB ADS (hold). E gun-mode if HasModeSwitch.
I inspect. E during inspect cycles highlighted attach. U configure popup.

E is context: fist = grab (12), melee-out = charge, gun focus = mode.
ADS is not RMB.
